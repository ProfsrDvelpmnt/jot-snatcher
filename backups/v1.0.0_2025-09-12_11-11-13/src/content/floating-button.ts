// Floating button script for JOT Snatcher Chrome Extension

import { FloatingButtonContainer } from '@/components/FloatingButton';

// This would be used if we need a separate floating button script
// For now, the floating button is handled in the content script

console.log('Floating button script loaded');

// Export for potential use in other contexts
export { FloatingButtonContainer };
